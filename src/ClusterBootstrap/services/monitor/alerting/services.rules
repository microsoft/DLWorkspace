groups:
    - name: services
      rules:
      - alert: ServiceNotUp
        expr: up != 1
        for: 30m
        labels:
          type: pai_service
        annotations:
          summary: "{{$labels.pai_service_name}} in {{$labels.instance}} not up detected"
