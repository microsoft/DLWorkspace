version: '2'
services:
  kibana:
    image: docker.elastic.co/kibana/kibana:7.3.1
    network_mode: "host"
    environment:
      SERVER_NAME: kibana.example.org
      ELASTICSEARCH_HOSTS: http://elastic-0:9200
      #    volumes:
            #      - ./kibana.yml:/usr/share/kibana/config/kibana.yml
    ports:
      - 5601:5601
