import json
import os
import re
import math

import yaml

# GlusterFSYaml is a class used to assist the generation of the yaml file required to deploy a 
# GlusterFS. 
# nodesinfo: partition of nodes as shown 
class GlusterFSYaml:
	def __init__(self, nodesinfo, )
